# API Documentation

The API provides several endpoints for interacting with the system:

## Endpoints

- GET /users - Get all users
- POST /users - Create new user
- GET /users/:id - Get user by ID

## Authentication

Use Bearer tokens for authentication.
